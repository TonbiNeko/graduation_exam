<h1><%= @user.name %>さんのごみ拾い記録一覧</h1>

<table>
  <thead>
    <tr>
      <th>画像</th>
      <th>タイトル</th>
      <th>内容</th>
    </tr>
  </thead>

  <tbody>
    <% @blogs.each do |blog| %>
      <tr>
        <td><%= image_tag blog.image.url if blog.image.url.present? %></td>
        <td><%= blog.title %></td>
        <td><%= blog.content %></td>
        <td><%= link_to '詳細', blog_path(blog) %></td>

        <td><%= link_to '編集', edit_blog_path(blog) %></td>
        <td><%= link_to '削除', blog, method: :delete, data: { confirm: '本当に削除してよろしいですか？' } %></td>
        
      </tr>
    <% end %>
  </tbody>
</table>